#include<graphics.h>
#include<conio.h>

int main()
{   
    int width = getmaxwidth();
    int height = getmaxheight();
    initwindow(width, height,"fillellipse");
    setbkcolor(BLACK);
    cleardevice();
    int x = 250;
    int y = 400;
    int col = 1;
    setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;
    setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;
    setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;
    setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;
    setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;
    setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;
    setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;
    setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;
    setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;
    setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;
    setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;
    setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;
    setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;
    setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;
    setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;
    setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;
    setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;
    setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;
    setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;
    setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;
    setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;
    setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;
    setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;
    setfillstyle(SOLID_FILL,col%16);
    fillellipse(width/2,height/2,x,y);
    delay(50);
    x=x-10;
    y=y-10;
    col++;
    
    getch();
    closegraph();
    return 0;
}